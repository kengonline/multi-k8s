{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker: Attach to Node",
            "type": "node",
            "request": "attach",
            "protocol": "auto",
            "port": 9229,
            "restart": true,
            "localRoot": "${workspaceFolder}/backend-server",
            "remoteRoot": "/app"
        }
    ]
}